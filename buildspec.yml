version: 0.2

# env:
#   variables:
#     AWS_REGION: "eu-north-1"
#     EKS_CLUSTER_NAME: "pdf-app-cluster"
#     BACKEND_REPO: "pdf-backend"
#     FRONTEND_REPO: "pdf-frontend"

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - echo "Installing kubectl..."
      - curl -fsSL "https://dl.k8s.io/release/$(curl -fsSL https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl" -o kubectl
      - chmod +x kubectl
      - mv kubectl /usr/local/bin/

  pre_build:
    commands:
      - export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
      - COMMIT_HASH=$(echo "$CODEBUILD_RESOLVED_SOURCE_VERSION" | cut -c 1-7)
      - export IMAGE_TAG_SHORT=${COMMIT_HASH:-latest}
      - export REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
      - echo "Region=$AWS_REGION Cluster=$EKS_CLUSTER_NAME Registry=$REGISTRY Tag=$IMAGE_TAG_SHORT"
      - aws ecr get-login-password --region "$AWS_REGION" | docker login --username AWS --password-stdin "$REGISTRY"
      - aws eks update-kubeconfig --region "$AWS_REGION" --name "$EKS_CLUSTER_NAME"

  build:
    commands:
      - echo "Building backend image..."
      - docker build -f Dockerfile -t "$REGISTRY/$BACKEND_REPO:$IMAGE_TAG_SHORT" .
      - docker tag "$REGISTRY/$BACKEND_REPO:$IMAGE_TAG_SHORT" "$REGISTRY/$BACKEND_REPO:latest"

      - echo "Building frontend image..."
      - docker build -f frontend.Dockerfile -t "$REGISTRY/$FRONTEND_REPO:$IMAGE_TAG_SHORT" .
      - docker tag "$REGISTRY/$FRONTEND_REPO:$IMAGE_TAG_SHORT" "$REGISTRY/$FRONTEND_REPO:latest"

      - echo "Pushing container images to ECR..."
      - docker push "$REGISTRY/$BACKEND_REPO:$IMAGE_TAG_SHORT"
      - docker push "$REGISTRY/$BACKEND_REPO:latest"
      - docker push "$REGISTRY/$FRONTEND_REPO:$IMAGE_TAG_SHORT"
      - docker push "$REGISTRY/$FRONTEND_REPO:latest"

  post_build:
    commands:
      - echo "Rendering Kubernetes manifest..."
      - |
        sed -e "s|\${REGISTRY}|$REGISTRY|g" \
            -e "s|\${BACKEND_REPO}|$BACKEND_REPO|g" \
            -e "s|\${FRONTEND_REPO}|$FRONTEND_REPO|g" \
            -e "s|\${IMAGE_TAG_SHORT}|$IMAGE_TAG_SHORT|g" \
            app-deployment.yaml > deploy.yaml
      - grep "image:" deploy.yaml
      - aws sts get-caller-identity
      - kubectl config current-context
      - kubectl get nodes
      - kubectl apply -f deploy.yaml
      - kubectl rollout status deployment/pdf-backend --timeout=180s
      - kubectl rollout status deployment/pdf-frontend --timeout=180s

artifacts:
  files:
    - deploy.yaml